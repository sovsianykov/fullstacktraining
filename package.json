{
  "name": "fullstack-traning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "herocu-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --client "    

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
